#include<iostream>
#include<cmath>
using namespace std;
int main()
{
    int n;
    double l;
    int t;
    while(cin>>n)
    {
        l;
        l=1/sqrt(5)*(  pow((0.5 + 0.5*sqrt(5) ),n+1) -  pow((0.5 +-0.5*sqrt(5) ),n+1)  );
        t = (int)l;
    }
    return 0;
}
